export type Group = {
  id: string;
  name: string;
  createdBy: string;
  inviteCode: string;
  createdAt: string;
};

export type Member = {
  userId: string;
  name: string;
  joinedAt: string;
};
